import {
    Card,
    styled,
} from '@material-ui/core';

export const StyledCard = styled(Card)({
    cursor: 'pointer',
    backgroundColor: 'rgb(93,138,168)',
    width: '250px',
    height: '250px',
});
